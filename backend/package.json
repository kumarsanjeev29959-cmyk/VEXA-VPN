{
  "name": "vexa-vpn-control-plane",
  "version": "0.1.0",
  "private": true,
  "description": "VEXA VPN device provisioning control plane",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
